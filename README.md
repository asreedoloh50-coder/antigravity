# 📚 ระบบตรวจงานพร้อมให้คะแนนออนไลน์

## 🎯 ภาพรวม

ระบบตรวจงานพร้อมให้คะแนนออนไลน์สำหรับโรงเรียน รองรับ 4 บทบาทผู้ใช้:
- **ครู**: สร้างห้องเรียน, มอบหมายงาน, ตรวจงาน, ให้คะแนน
- **นักเรียน**: เข้าร่วมห้องเรียน, ส่งงาน, ดูคะแนน
- **ผู้ปกครอง**: เชื่อมต่อกับนักเรียน, ดูคะแนนและความก้าวหน้า
- **ผู้ดูแลระบบ**: จัดการผู้ใช้, ดู Logs, สำรองข้อมูล

## ✨ คุณสมบัติหลัก

- ✅ PWA - ติดตั้งบนมือถือได้ทั้ง Android และ iOS
- ✅ Responsive UI - ใช้งานได้ทั้งมือถือและเดสก์ท็อป
- ✅ Multi-device - ใช้งานได้หลายเครื่องพร้อมกัน
- ✅ Demo Mode - ทดลองใช้งานได้ทันทีไม่ต้อง setup
- ✅ RBAC - ระบบสิทธิ์แยกตามบทบาท
- ✅ Session Management - จัดการ session พร้อมหมดอายุ
- ✅ Password Hashing - เก็บรหัสผ่านอย่างปลอดภัย
- ✅ Workflow สถานะงาน - รองรับการส่งแก้ไข
- ✅ File Upload - อัปโหลดไฟล์ไปยัง Google Drive
- ✅ CSV Export - ส่งออกคะแนนและรายการค้างส่ง
- ✅ Pagination & Search - รองรับข้อมูลจำนวนมาก

---

## 🚀 การติดตั้งและ Deploy

### ขั้นตอนที่ 1: สร้าง Google Sheets Database

1. ไปที่ [Google Apps Script](https://script.google.com/)
2. สร้าง Project ใหม่
3. คัดลอกโค้ดจาก `backend/Code.gs` ไปวาง
4. รัน function `setupDatabase()` เพื่อสร้าง Spreadsheet
5. จด Spreadsheet ID ที่ได้
6. แก้ไข `CONFIG.SPREADSHEET_ID` ใน Code.gs ให้เป็น ID ที่ได้
7. สร้าง Google Drive Folder สำหรับเก็บไฟล์ และใส่ ID ใน `CONFIG.DRIVE_FOLDER_ID`

### ขั้นตอนที่ 2: Deploy Google Apps Script Web App

1. ใน Apps Script Editor กด **Deploy → New deployment**
2. เลือก Type: **Web app**
3. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. กด **Deploy**
5. คัดลอก **Web App URL**

### ขั้นตอนที่ 3: ตั้งค่า Frontend

1. เปิดไฟล์ `js/api.js`
2. แก้ไข `BASE_URL` ให้เป็น Web App URL ที่ได้:
```javascript
BASE_URL: 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec',
```

### ขั้นตอนที่ 4: Deploy Frontend

#### Option A: Netlify
1. Push โค้ดขึ้น GitHub
2. ไปที่ [Netlify](https://netlify.com) และเชื่อมต่อ Repository
3. ตั้งค่า:
   - Build command: (ว่าง)
   - Publish directory: `.`
4. Deploy

#### Option B: Vercel
1. Push โค้ดขึ้น GitHub
2. ไปที่ [Vercel](https://vercel.com) และ Import Project
3. Deploy

---

## 🧪 ทดลองใช้งาน (Demo Mode)

ระบบมี Demo Mode ที่ใช้ localStorage ทำให้ทดลองได้ทันทีไม่ต้อง setup backend

### บัญชีทดลอง:
| บทบาท | อีเมล | รหัสผ่าน |
|-------|-------|----------|
| ครู | teacher@demo.com | 1234 |
| นักเรียน | student@demo.com | 1234 |
| ผู้ปกครอง | parent@demo.com | 1234 |
| แอดมิน | admin@demo.com | 1234 |

### วิธีสลับโหมด:
1. เข้าสู่ระบบด้วยบัญชีใดก็ได้
2. ไปที่ **ตั้งค่า**
3. กดปุ่ม **สลับโหมด**

---

## 📱 การติดตั้ง PWA

### Android (Chrome)
1. เปิดเว็บไซต์ใน Chrome
2. กดปุ่ม **ติดตั้ง** ที่แสดงขึ้น
3. หรือไปที่เมนู → **Add to Home screen**

### iOS (Safari)
1. เปิดเว็บไซต์ใน Safari
2. กดปุ่ม **Share** (ไอคอนกล่องพร้อมลูกศร)
3. เลือก **Add to Home Screen**
4. กด **Add**

---

## 🔧 โครงสร้างโปรเจกต์

```
/
├── index.html              # หน้าหลัก
├── manifest.json           # PWA manifest
├── service-worker.js       # Service Worker
├── netlify.toml           # Netlify config
├── vercel.json            # Vercel config
├── css/
│   └── styles.css         # สไตล์ทั้งหมด
├── js/
│   ├── app.js             # Main App & Routes
│   ├── router.js          # SPA Router
│   ├── auth.js            # Authentication
│   ├── api.js             # API Handler
│   ├── store.js           # State Management
│   ├── teacher.js         # Teacher Module
│   ├── student.js         # Student Module
│   ├── parent.js          # Parent Module
│   ├── admin.js           # Admin Module
│   ├── utils.js           # Utility Functions
│   └── i18n.js            # Internationalization
├── assets/
│   └── icons/
│       ├── icon-192.png   # App Icon 192px
│       └── icon-512.png   # App Icon 512px
└── backend/
    └── Code.gs            # Google Apps Script
```

---

## 📋 API Endpoints

### Authentication
- `register` - สมัครสมาชิก
- `login` - เข้าสู่ระบบ
- `logout` - ออกจากระบบ
- `me` - ข้อมูลผู้ใช้ปัจจุบัน

### Classes & Subjects
- `createClass` - สร้างห้องเรียน
- `listClasses` - รายการห้องเรียน
- `joinClass` - เข้าร่วมห้องเรียน
- `createSubject` - เพิ่มรายวิชา
- `listSubjects` - รายการรายวิชา

### Assignments
- `createAssignment` - สร้างงาน
- `listAssignments` - รายการงาน
- `getAssignment` - รายละเอียดงาน

### Submissions
- `submitHomework` - ส่งงาน
- `listSubmissionsByAssignment` - งานที่ส่งตามงาน
- `listSubmissionsByStudent` - งานที่ส่งตามนักเรียน
- `requestRevision` - ขอให้แก้ไข

### Grading
- `gradeHomework` - ให้คะแนน
- `getStudentGrades` - คะแนนนักเรียน

### Parent
- `parentLink` - เชื่อมต่อลูก
- `getLinkedStudents` - รายการลูกที่เชื่อมต่อ
- `parentGetGrades` - คะแนนของลูก

### Admin
- `listUsers` - รายการผู้ใช้
- `updateUser` - แก้ไขผู้ใช้
- `getAuditLogs` - บันทึกระบบ
- `backupData` - สำรองข้อมูล
- `restoreData` - กู้คืนข้อมูล

### Export
- `exportGradesCSV` - ส่งออกคะแนน CSV
- `exportMissingCSV` - ส่งออกค้างส่ง CSV

---

## 🧪 การทดสอบ End-to-End

### 1. ทดสอบครู
1. เข้าสู่ระบบด้วย `teacher@demo.com / 1234`
2. สร้างห้องเรียนใหม่
3. เพิ่มรายวิชา
4. สร้างงานใหม่
5. ตรวจงานที่ส่ง (ถ้ามี)
6. ดูสมุดคะแนน
7. ส่งออก CSV

### 2. ทดสอบนักเรียน
1. เข้าสู่ระบบด้วย `student@demo.com / 1234`
2. เข้าร่วมห้องเรียนด้วยรหัส `ABC123`
3. ดูรายการงาน
4. ส่งงาน
5. ดูคะแนน

### 3. ทดสอบผู้ปกครอง
1. เข้าสู่ระบบด้วย `parent@demo.com / 1234`
2. ดูคะแนนลูกที่เชื่อมต่อแล้ว
3. ลองเชื่อมต่อลูกเพิ่ม (ถ้ามี linkCode)

### 4. ทดสอบแอดมิน
1. เข้าสู่ระบบด้วย `admin@demo.com / 1234`
2. ดูรายการผู้ใช้
3. แก้ไขผู้ใช้
4. ดู Audit Logs
5. สำรอง/กู้คืนข้อมูล

### 5. ทดสอบ PWA
1. เปิดเว็บไซต์บนมือถือ
2. ติดตั้งแอป
3. เปิดแอปจากหน้าจอโฮม
4. ทดสอบ offline

---

## 📊 Schema ฐานข้อมูล

### users
| Column | Type | Description |
|--------|------|-------------|
| id | string | Primary Key |
| role | string | teacher/student/parent/admin |
| name | string | ชื่อ-นามสกุล |
| email | string | อีเมล (unique) |
| passwordHash | string | รหัสผ่านที่ hash แล้ว |
| salt | string | Salt สำหรับ hash |
| createdAt | datetime | วันที่สร้าง |
| isActive | boolean | สถานะใช้งาน |

### classes
| Column | Type | Description |
|--------|------|-------------|
| id | string | Primary Key |
| name | string | ชื่อห้อง |
| teacherId | string | FK → users.id |
| joinCode | string | รหัสเข้าร่วม (unique) |
| createdAt | datetime | วันที่สร้าง |
| isActive | boolean | สถานะใช้งาน |

(ดูเพิ่มเติมใน backend/Code.gs ฟังก์ชัน setupDatabase)

---

## 🔒 ความปลอดภัย

- **Password Hashing**: ใช้ SHA-256 + Salt
- **Session Token**: สุ่ม 64 ตัวอักษร พร้อมวันหมดอายุ
- **RBAC**: ตรวจสอบสิทธิ์ทุก API
- **Input Validation**: ตรวจสอบข้อมูลก่อนบันทึก
- **File Validation**: ตรวจสอบประเภทและขนาดไฟล์

---

## 📝 License

MIT License - ใช้งานได้อิสระ

---

## 🤝 ผู้พัฒนา

พัฒนาโดย AI Antigravity ตามความต้องการของผู้ใช้
